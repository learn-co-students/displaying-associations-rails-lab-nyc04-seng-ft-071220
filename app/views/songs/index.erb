<h1>Song Index</h1>
<div>
<ul>
    <% @songs.each do |song| %>
        <li>
            <a href="<%= song_path(song) %>">
                 <%= song.artist_name%> - <%= song.title %>
            </a>
        </li>
    <% end %>
</ul>
</div>