<h1> Song Index </h1>
<ul>
<% @songs.each do |song| %>
    <li>
        <a href="<%=song_path(song) %>">      
            <%= song.artist_name %> 
            <span> - </span>
            <%= song.title %> 
        </a>
    </li>
        <% end %>
</ul>
    

